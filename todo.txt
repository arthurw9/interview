Forms:
- Get values from a different form and/or copy.

GUI:
- Make the navigation and developer buttons explicit commands: NextPage, PrevPage, Developer
- Table columns made by the select statement should resizeable by the user.
- Input boxes made by the input statements should be resizeable by the user.
- Add a refresh button (footer pages complicate what gets refreshed. Add gosub?)

Language:
- Delete form copy
- if statements.
- loops.
- Allow reading additional code from other files while retaining form/data state.
- Display a PDF
- List PDF form fields
- Fill out a PDF form and display it.

Database Features:
- Create sum function to sum values from all copies of a form.
- Create count function to count copies of a form.
- Create where clause for sum and count functions
- Create foreign key constraints between forms.

IDE features:
- Create a graphical designer that creates interview code.

Tutorial
- Split the Tutorial into separate pages with small bits of the model in each page.
- Create example applications in separate pages.
- Describe the _id keyword in the tutorial.

